# Slim requirements for cloud deployment (Render free tier).
#
# Default cloud image has the rules-fallback parser only (10/10 accuracy,
# no API key needed). To use an LLM in the cloud image, install its SDK
# in the image AND set the corresponding env var on Render.

# --- Web framework + LP solver ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pulp>=2.7

# --- LLM SDKs (small, kept in image so toggling GRIDWISE_USE_GROQ=1 or
#     GRIDWISE_USE_GEMINI=1 in the Render dashboard Just Works) ---
groq>=0.11
google-genai>=0.3

# --- Optional ML stack (uncomment to ship the local Qwen LLM) ---
# torch>=2.0
# transformers>=4.40
# peft>=0.10
# accelerate>=0.27
