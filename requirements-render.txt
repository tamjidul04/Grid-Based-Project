# Slim requirements for cloud deployment (Render free tier).
#
# Same runtime minus the heavy ML stack — torch/transformers/peft/trl are
# excluded because:
#   - The local Qwen2.5 model is too inaccurate on CPU to ship as primary.
#   - On Render we use Gemini API (just needs `google-genai`).
#   - Rules fallback has 10/10 accuracy and runs in 10ms.
#
# If you want the local LLM in the cloud image, uncomment the ML block.

# --- Web framework + LP solver ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pulp>=2.7

# --- Gemini API client (used when GRIDWISE_USE_GEMINI=1) ---
google-genai>=0.3

# --- Optional ML stack (uncomment for local LLM in cloud image) ---
# torch>=2.0
# transformers>=4.40
# peft>=0.10
# accelerate>=0.27
